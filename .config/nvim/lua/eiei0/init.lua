-- Plugin manager (uncomment one)
require("eiei0.lazy")     -- lazy.nvim (default)
-- require("eiei0.packer") -- packer.nvim (legacy - also need to run :PackerSync after uncommenting)

require("eiei0.set")
require("eiei0.remap")
